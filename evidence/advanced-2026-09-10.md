# Advanced submits — 2026-09-10 (issue #61, roadmap §13.8)

Contract `CDSAQVJCHBDQ6SGXYHXBIAWPN4BM4BAR6FE3HMBFO6WOTKBGQ7B2MJWZ`
(pre-state: epoch_count=2 — epoch 0 canonical base, epoch 1 two-signer multi).

## Self-registered customer keys (FIX 1 pattern, one tx per member)

| Member | Customer address | Tx | Ledger |
|---|---|---|---|
| tessera-member-a | `GCPXB3…AQ` | `d94773535bc039186854c63c482a4a8470a5e098e23eb4abc0c9e8abdaff6f3e` | 4603213 |
| tessera-member-b | `GD7ZRP…56EU` | `d4de2d286a2e09373594d5927c56e475b595fb41d914078c8c8dbc6de1157b51` | 4603215 |
| tessera-member-c | `GCBCMG…INC7` | `d6aa106475d1ad60d935a7dcb78794ebd6a35a16ea3ab609aaa89a2229cc4082` | 4603217 |
| tessera-member-d | `GBHEI5…HVQ` | `e92fcc73f16bcbcb244d63761c26f9be1ccc20eb8939948f4b23c7c861838fb1` | 4603219 |

`registered_key_count` read back: `4`. Keys = honest signed fixture keys
(`contracts/artifacts/signed-incircuit-args.json`).

## submit_signed_attestation HONEST — LIVE

- Tx `66cbf46f69efed8e484856fbe73b9c6092a567d23a3728b9c4cb7a17a39cdcbe`
  (ledger 4603225, successful=true), source `tessera-treasury`.
- Stored epoch 2: root `08ac11ac…1012`, total `28000` (`0x6d60`), reserves
  `30000` (`0x7530`), bound `189140` @ ledger 4603225, `control_proven:true`,
  `non_omission_in_circuit:true`. `signed_epoch`=0, `epoch_count`=3.

## submit_risk_attestation — REJECTED LIVE with Error #20 (BY DESIGN)

- Simulation: `HostError: Error(Contract, #20)` (`WeakAttestationDowngrade`),
  nothing stored. Fixture declares reserves 118800 (backed), proof valid —
  the rejection is pure ordering: `submit_risk_attestation` mirrors a
  `non_omission=false` root into `Latest`, and `Latest` currently holds the
  signed `non_omission=true` root from the tx above. The contract refuses to
  downgrade the bound root. **Downgrade protection confirmed firing on a live
  contract.**
- SEQUENCING LESSON (runbook-grade): on any contract instance, submit RISK
  before SIGNED if both attestations are wanted — after a signed Latest,
  `RiskLatest` can never be stored (no path clears the flag). Whether the risk
  path should store `RiskLatest` WITHOUT mirroring a weaker root into `Latest`
  is tracked as a follow-up (filed separately) — NOT changed here.

## Negative paths (simulations, no state change — all exact)

| Case | Result |
|---|---|
| Omission proof (filler at slot C) | `Error(Contract, #10)` RegisteredSetMismatch |
| Replay of the honest signed proof | `Error(Contract, #17)` RootAlreadyAttested |
| Tampered proof (A↔C swap) | `Error(Contract, #1)` InvalidSolvencyProof |
| Risk after signed Latest | `Error(Contract, #20)` WeakAttestationDowngrade |

## Scripts used (no new scripts — existing artifacts)

- `contracts/artifacts/signed-incircuit-args.json` (proof + keys),
  `contracts/artifacts/risk-args.json`; `--public_signals-file-path` form
  (vec args need the file-path flag, cf. #59).
